
import API from '../../../../core/config/axios';
import { GeofencingRepository } from '../../domain/repositories/GeofencingRepository';

export const axiosGeofencingRepository: GeofencingRepository = {
    // Implementar métodos aquí usando API.get(), API.post(), etc.
};
