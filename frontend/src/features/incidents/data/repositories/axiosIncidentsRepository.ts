
import API from '../../../../core/config/axios';
import { IncidentsRepository } from '../../domain/repositories/IncidentsRepository';

export const axiosIncidentsRepository: IncidentsRepository = {
    // Implementar métodos aquí usando API.get(), API.post(), etc.
};
